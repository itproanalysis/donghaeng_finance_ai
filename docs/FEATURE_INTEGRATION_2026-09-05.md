# Feature 통합 및 공개 서비스 반영 · 2026-09-05

## Git 반영

- 소스: `origin/feature/demo-scenario` · `aee1dda6df7b65fc69f0b70fd6b58a6564ca9788`.
- 부모 `da9cd32` 이후 28개 파일 변경분을 현재 작업 내용과 파일별 3-way 통합했다. 기존 디자인·기획안 반영, 방문자 인증, 모델링 근거 기능을 보존했다.
- README 문서 목록 충돌은 양쪽 목록을 유지하여 해결했다. 기존 작업 폴더의 스테이징 상태와 브랜치 이력은 보존했다. 후속 Git push 및 최종 배포 상태는 [9월 6일 배포 기록](RELEASE_2026-09-06.md)을 따른다.
- 통합 전 백업: 사용자 임시 폴더의 `donghaeng-feature-aee1dda-iaqh_h8d` (manifest 포함).

## 최종 동작

1. 홈과 서비스 소개에서 **답변 → 변수 → 점수 시연**을 연다. 가상 인물·합성 거래자료를 알리고 AI 처리 동의를 직접 받는다.
2. 등록된 12개 필드 중 기본 8개와 조건부 영업일 질문에 답하는 9턴 시연이다. 추가 참고 3개는 미확인 상태를 보존한다. 자동 입력은 사용자가 버튼을 누른 뒤에만 실행한다.
3. FINAL 답변을 모델 필드·단위로 옮겨 Python의 기존 두 축 규칙으로 계산한다. 원문에 없는 값을 임의로 채우지 않는다.
4. 완료 화면과 담당자 검토 화면이 같은 API 결과와 실제 배점·분모를 보여 준다. 실패 시 재시도할 수 있다.
5. 일반 상담에는 합성 거래자료를 연결하지 않는다. API는 기존 방문자별 권한을 먼저 확인한다.
6. 기획안의 목표·수행기록·재평가·검토 요약을 제공하며 실제 기관 전송은 구현 범위에 포함하지 않는다.

## 통합 중 보완

- 저장된 한글 업종명을 카탈로그 코드로 변환해 잘못된 업종 매핑을 방지했다.
- 추가된 질문을 Claude의 구조화 도구 규격에도 반영했다.
- 취소된 자동 입력 타이머가 답변 완료로 처리되던 문제를 수정했다.
- Python 환경과 합성 입력자료를 GCP 실행 이미지에 포함하고, 데이터 볼륨과 겹치지 않는 경로에 둔다.
- 임시 답변 파일은 계산 후 정리한다. 재요청 캐시, 동일 계산 중복 방지, 동시 2건 제한, 실행 시간 제한을 적용했다.
- Next가 동적 임시 경로를 저장소 전체 파일로 추적하던 빌드 경고를 제거했다.

## 검증

- 전체 Vitest: **105개 파일, 757개 통과**. 웹 진입 질문 우선순위 회귀 검사를 포함한다.
- TypeScript, ESLint, 프로덕션 빌드 통과.
- 기존 E2E: 권한·격리·동의·상담 저장·API·HTTP/SSE/WS 경로 통과.
- 새 시연 E2E: 15/15 통과. 프로덕션 빌드에서 로컬 질문 공급자 응답과 실제 Python 계산을 사용했다.
- 같은 합성 거래자료: 현재 상황 78/78, 개선가능성 67.5/30. 같은 FINAL 재조회 결과 동일.
- 목표: 23.6667 → 30.6667일, 목표 29일 충족. 수행자료도 합성 시연이며 실제 고객 실적이 아니다.
- Python 기존 검사 82/82, 구간 39/52, 모델 산출물 체크섬 `da713d7e95071afb55150a595a59e8b61dff8e8e028894ef46acf93c7ab60bd1`.

## 배포

- 대상: `donghaeng-finance-review` → `donghaeng-review-app`.
- 9월 5일 배포 이미지: `5dbeaa95-6a8b-4696-b5cc-ea250106539f`. Digest: `sha256:db11f7ea223c97307b63d52ce0b14f842c626d775d405b615c145c5df2f8a427`. 후속 최종 배포는 [9월 6일 기록](RELEASE_2026-09-06.md)을 따른다.
- `6f486f1e-68d0-4ab8-9573-993657da51e9`는 중간 검증에 사용했다. `461cc7c5-ac2c-49fc-93a9-b2d6ecb83f4e`는 배포하지 않았다.
- 보호된 운영자 서비스와 Google IAP 설정은 변경 대상이 아니다.

## 공개 검증에서 확인한 내용

- 웹 진입부에서 모든 선택 질문을 P2로 내리던 처리로 추가 질문이 생략되는 문제를 발견했다. 조건부 질문 P0를 보존하고 웹 진입과 E2E에서 `createBorrowerRequiredInformationList`를 공유하도록 수정했다. 일반 음식점 8항목·카페 11항목 경로는 유지한다.
- 실제 공개 AI 공급자와 Python 계산으로 primary/control FINAL을 생성해 **15/15 통과**했다. `npm run verify:demo -- --public-live <공개 review URL>`로 재현한다. 생성 데이터는 모두 등록된 합성 시연이다.
- 최종 배포 후 공개 접근·동의·방문자 재사용·교차 방문자 HTTP/SSE/WS 거부 검사 통과.
- PC·모바일에서 첫 화면, 소개, 변수 검색, 목표·수행기록, 재평가, 검토 요약을 확인했다. 모바일 390px에서 문서 가로 넘침이 없었다.
- 목표·자료 확인 기록과 검토 의견은 새로고침 후 유지됐고, 다른 사례로 바꾸면 별도 기록이 표시됐다.
- Chrome에서 `case_operating_drop_after-review.json`이 실제 다운로드된 것을 확인했다. 최초·재평가의 네 축 산식과 분모, 94개 변수, 6개 월별 수행기록을 파일에서 대조했다. 브라우저 자동화 download 이벤트는 오지 않았으나 파일 저장은 정상이다. PDF 인쇄물의 시각 검증은 수행하지 않았다.
- 최종 답변 기록을 다시 열어 ‘영업일 감소 사유 · 건강 · 현재 해소됨’과 참고 항목의 한글 이름을 확인했다. 담당자 화면도 같은 78/67.5 결과를 표시했다.
